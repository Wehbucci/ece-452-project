<?xml version="1.0" encoding="utf-8"?>
<!-- The tile the sprout sits on. A flat fill on purpose: the launcher masks this layer
     into whatever shape the device uses, so any artwork here would be cropped by it. -->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="108dp"
    android:height="108dp"
    android:viewportWidth="108"
    android:viewportHeight="108">
    <!-- Grasp Lilac Background (PathScreenBg) -->
    <path
        android:fillColor="#F3F2FB"
        android:pathData="M0,0h108v108h-108z" />
</vector>
